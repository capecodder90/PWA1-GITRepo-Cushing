/**
 * Name: Sarah Cushing
 * Date: December 17, 2014
 * Class: PWA
 * Goal: Goal7
 */

// declare global variables including:
    // "names", array with names of 5 people
    // "instances", maximum number of instances of the Person object (3)
    // "people", empty array
    // "interval", empty variable

// create functions including:
    // "populateHTML",
        // use innerHTML to inject the names and jobs of the people
    // "runUpdate",
        // create "update" function to loop through each Person instance stored in the People array

// create a for loop (using "instances" for the comparison)
    // create a random number between 1 and 5 (number of names in array) using Math.random, store it in the variable "someName"
    // create Person object (var person = new Person(names[someName], someRow)), store in "people" array
    // call "populateHTML" function
    // don't allow duplicate names to appear on the web page

// call "runUpdate" function, set to run 30 times per second

